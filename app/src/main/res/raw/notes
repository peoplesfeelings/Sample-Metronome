for release
    version data
        mani
        gradl
        string
    github

replicate

to do
    style status bar color

check
    does service.loadFile fail gracefully on first start, before permission is granted